apiVersion: v1
kind: Service
metadata:
  name: aigenius-ticketing
  labels:
    app: aigenius-ticketing
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: aigenius-ticketing
